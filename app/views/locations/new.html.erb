<h2>Create new location</h2>

<%= form_with( model: location , local: true) do |f| %>
    <p>
        <%= f.label :name %> <br/>
        <%= f.text_field :name %>
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>

<%= link_to  "Back", root_path %>